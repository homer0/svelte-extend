# svelte-extend - Rollup example

This is a basic example of how to use `svelte-extend` with Rollup.

You can run it with:

```bash
# Install the dependencies
npm ci
# Start the dev server
npm run dev
```
